{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun run build"
  },
  "deploy": {
    "startCommand": "bunx serve dist/spa -l ${PORT:-3000} -s"
  }
}
