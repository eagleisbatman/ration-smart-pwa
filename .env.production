# Production Environment
# Used when running: bun build (Railway deployment)
#
# IMPORTANT: These values are baked in at BUILD TIME by Vite.
# To override, set these environment variables in Railway dashboard:
# - VITE_API_BASE_URL (your backend Railway URL)
# - VITE_APP_API_KEY (if needed)
# - VITE_VAPID_PUBLIC_KEY (for push notifications)

# Default to empty - MUST be set in Railway dashboard
VITE_API_BASE_URL=
VITE_APP_API_KEY=
VITE_APP_ENV=production
VITE_VAPID_PUBLIC_KEY=
