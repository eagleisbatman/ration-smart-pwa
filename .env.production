# Production Environment
# Used when running: bun build (Railway deployment)
# Railway will inject these from environment variables

VITE_API_BASE_URL=${RAILWAY_BACKEND_URL:-https://ration-smart-backend-production.up.railway.app}
VITE_APP_API_KEY=${APP_API_KEY}
VITE_APP_ENV=production

# Push Notifications
VITE_VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY}
